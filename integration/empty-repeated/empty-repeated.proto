syntax = "proto3";

package empty_repeated;

message TestMessage {
  repeated int32 numbers = 1;
  map<string, string> metadata = 2;
  repeated TestEnum enums = 3;
  repeated string project_manager = 4;
}

enum TestEnum {
  ZERO = 0;
  ONE = 1;
}
